{# begin {templatelocation} #}

<aside id="{area_id}">
  {!if is_comment_only_page}
  <h1>{comment_page_title}</h1>
  {!endif}
  
  <div id="comments">
    {commentbar}
    {comments}

    {!if pagenav}
    <div class="uk-margin-top">{pagenav}</div>
    {!endif}
  </div>
  {commenteditor}
</aside>

{# end {templatelocation} #}
